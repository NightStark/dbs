#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <string.h>

#include <ns_symbol.h>
#include <ns_type.h>
#include <ns_debug.h>
#include <ns_string.h>
#include <ns_lilist.h>

#include <ns_test.h>

NSTEST(XXXXXXXXXXXX)
{
	MSG_PRINTF("Test Function %s %d", __func__, (INT)arg);

	return;
}
NSTEST(SSSSSSSSSSSSSS)
{
	MSG_PRINTF("Test Function %s %d", __func__, (INT)arg);

	return;
}
NSTEST(VVVVVVVVVVVVVVV)
{
	MSG_PRINTF("Test Function %s %d", __func__, (INT)arg);

	return;
}
NSTEST(JJJJJJJJJJJJJJJ)
{
	MSG_PRINTF("Test Function %s %d", __func__, (INT)arg);

	return;
}
NSTEST(LLLLLLLLLLLLLLLLL)
{
	MSG_PRINTF("Test Function %s %d", __func__, (INT)arg);

	return;
}
NSTEST(FFFFFFFF)
{
	MSG_PRINTF("Test Function %s %d", __func__, (INT)arg);

	return;
}

